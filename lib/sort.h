template<typename T>
void merge_sort(T begin, T end);

template<typename I>
void merge(I begin, I mid_point, I end);

template <typename T> 
void insertion_sort(T start, T end);

template <typename T> 
void print_vector(T vec);

template <typename T>
void fill_vector_with_random(T& vec);
